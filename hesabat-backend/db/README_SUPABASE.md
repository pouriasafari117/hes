# سوپابیس

اگر دیتابیس از قبل جداول اعضا/وام دارد:

**فقط یک فایل را در SQL Editor اجرا کنید:** `supabase_now.sql`

- اسکیمای کامل قدیمی را دوباره نزنید.
- `complete_schema.sql` فقط برای دیتابیس خالی است.
- `020_portal.sql` همان محتواست برای migrate لوکال.

بعد از اجرا، پنل را یک‌بار رفرش کنید.
