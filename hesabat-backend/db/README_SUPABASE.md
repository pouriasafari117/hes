# سوپابیس — یک فایل

در SQL Editor **فقط** این را اجرا کنید:

`hesabat-backend/db/complete_schema.sql`

ادغام اسکیمای کامل + پورتال پنل است (`if not exists` / `add column if not exists`). داده را پاک نمی‌کند.

`supabase_now.sql` فقط وقتی که نمی‌خواهید complete را دوباره بزنید و پورتال هنوز ناقص است.

بعد از اجرا پنل را رفرش کنید.
